{
  "name": "bank-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ bank-subgraph",
    "create-local": "graph create --node http://localhost:8020/ bank-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ bank-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bank-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.29.1",
    "matchstick-as": "0.5.0"
  }
}
